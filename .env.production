# Production / Staging Environment Variables
# For Cloud Run: secrets are injected via Secret Manager (see infra/setup-gcp.sh)
# This file documents the expected variables and their staging values.

# Django Settings
DEBUG=False
ENVIRONMENT=production
# SECRET_KEY → via Secret Manager
DJANGO_ALLOWED_HOSTS=*.run.app

# Database - Neon PostgreSQL (use pooler endpoint)
# DATABASE_URL → via Secret Manager

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://bookofmonth.web.app

# Email Configuration (optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Django Q2 Workers (not used on Cloud Run - tasks run via Cloud Run Jobs)
Q_CLUSTER_WORKERS=2

# Logging
LOG_LEVEL=INFO
# SENTRY_DSN → via Secret Manager

# Frontend (set at build time via Vite)
# VITE_API_BASE_URL is passed during `npm run build` in deploy.sh
